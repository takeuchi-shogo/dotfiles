# Skill Audit Report — 2026-06-20

**Audit type**: Static scans (full) + A/B benchmark (existing 4 skills)
**Scope**: Batch 1+2 default (10 skills) → adjusted to **4 existing** (`react-best-practices`, `frontend-design`, `ui-ux-pro-max`, `review`) after 6/10 default skills not found
**Skill universe**: 122 SKILL.md files under `~/.claude/skills/`
**Usage log**: `skill-executions.jsonl` absent → Step 0.5 skipped
**Prior reference**: [2026-05-27 audit](2026-05-27-audit.md)

---

## Executive Summary

| Skill | 5D | CompDepth | Stale Refs | Quality Δ | Recommendation |
|-------|-----|-----------|------------|-----------|----------------|
| `review` | Good (M) | 1 (no inline chain) | — | _A/B failed (session limit)_ | **Keep** (only target with `## Gotchas`) |
| `react-best-practices` | Avg (OutcomesRubric + no Gotchas) | 1 | `/senior-frontend` (still missing) | partial: **−2 / 0 / —** (mean −1.0, N=2) | **Improve** (description + Gotchas + check if skill hurts on focused asks) |
| `frontend-design` | Avg (boundary vs ui-ux-pro-max) | 1 | `/senior-frontend` (still missing) | _A/B failed_ | **Improve** (mutual exclusion with ui-ux-pro-max) |
| `ui-ux-pro-max` | Avg (boundary + heavy desc 471 chars) | 1 | `/senior-frontend` (still missing) | partial: **+1** (N=1) | **Improve** (mutual exclusion + desc trim) |

> **Headline findings (static-only, A/B will refine but not change the direction)**:
> 1. **Gotchas coverage 14%** (17/122) — far below the 40% threshold, next audit goal = bring all skills above 40%.
> 2. **Description token tax = 41,169 chars** vs budget 1,000–1,500 chars (27–41× over) — system prompt bloat is real.
> 3. **frontend-design ↔ ui-ux-pro-max boundary still undeclared** — neither skill mentions the other in `Do NOT use for`. Same conflict flagged 2026-05-27 and unresolved.
> 4. **`/senior-frontend` stale reference** in all 3 frontend skills (`/senior-frontend` no longer exists in `~/.claude/skills/`).

---

## Step 0 — 5D Quality Scan

ratings: **Good** | **Average** | **Poor**

| Skill | Safety | Completeness | Executability | Maintainability | Cost | Outcomes-not-features |
|-------|--------|--------------|---------------|-----------------|------|----------------------|
| `react-best-practices` | Good | Good | Good | Good | Good | **Average** (description leads with "40+ rule" feature list) |
| `frontend-design` | Good | Good | Good | **Average** (build + 5 Refine ops + boundary undeclared) | Good | Good ("Create or refine ...") |
| `ui-ux-pro-max` | Good (read-only) | Good | Good | **Average** (10-stack scope) | **Average** (large KB lookups) | Good ("Use when designing or implementing UI/UX") |
| `review` | Good (PreToolUse hook blocks Edit/Write) | Good | Good | **Average** (orchestrates many reviewer agents) | **Average** (parallel reviewer fan-out) | Good ("コード変更のレビューを実行") |

- `react-best-practices`: **(OutcomesRubric)** — description starts with a feature noun ("React/Next.js パフォーマンス最適化 40+ rule"), not an outcome verb
- `review` is the only target skill with a `## Gotchas` section

---

## Step 0.5 — Usage Tier Classification

**SKIPPED** — `~/.claude/agent-memory/skill-executions.jsonl` does not exist this session. The previous audit (2026-05-27) found `review` Borderline Dominant (31.4%) and the other 3 Unused; that signal is still the best-available baseline.

> **Action**: enable `skill-executions.jsonl` capture before the next audit, or document where Skill execution telemetry now lives.

---

## Step 0.6 — Description Token Tax

| Metric | Value | Status |
|--------|-------|--------|
| Skill count (descriptions parsed) | 122 | — |
| Total description chars | **41,169** | 🚨 27–41× over budget (1,000–1,500 char target) |
| Heaviest single skill | `design-md-init` (724 chars) | over 200-char single-skill limit |

### Top 10 heaviest descriptions

| Rank | Skill | Chars | Action |
|------|-------|-------|--------|
| 1 | `design-md-init` | 724 | trim — long compound description |
| 2 | `image-to-code-skill` | 673 | trim |
| 3 | `imagegen-frontend-web` | 661 | trim |
| 4 | `imagegen-frontend-mobile` | 638 | trim |
| 5 | `obsidian-knowledge` | 574 | trim |
| 6 | `audit` | 555 | trim |
| 7 | `google-cloud-waf-security` | 537 | trim |
| 8 | `dispatch` | 527 | trim |
| 9 | `frontend-design` | 488 | trim — target skill |
| 10 | `google-cloud-recipe-auth` | 483 | trim |

> **Pruning policy reference**: [`docs/specs/2026-05-04-skill-tier-pruning.md`](../specs/2026-05-04-skill-tier-pruning.md). Average description = ~337 chars; bringing all skills ≤200 chars would cut tax to ~24,000 chars (still 16× over budget — structural pruning of skills also required).

---

## Step 0.7 — Composition Depth Check

| Skill | Agent/Skill calls in SKILL.md | Depth | Notes |
|-------|-------------------------------|-------|-------|
| `react-best-practices` | 0 | 1 | reference-driven; no chain |
| `frontend-design` | 0 | 1 | reference-driven; no chain |
| `ui-ux-pro-max` | 0 | 1 | KB + CLI driven; no chain |
| `review` | 0 in SKILL.md text (orchestrates code-reviewer/etc. at runtime via the runtime gate, not via inline `Skill(...)`/`Agent(...)` invocations parseable from SKILL.md) | 1 (static), 2–3 (runtime) | runtime depth not over-ceiling |

No skill exceeds the depth-9 over-ceiling threshold.

---

## Trigger Conflict Scan (full)

| Class | Count |
|-------|-------|
| Exact-match phrase across multiple skills | **2** |
| Partial-include (substring) pairs | **28** |
| `Do NOT use for` exclusion present where overlap exists | ~partial |

### Exact-match conflicts

| Phrase | Skills | Severity | Action |
|--------|--------|----------|--------|
| `バックグラウンドで` | `autonomous`, `dispatch` | Medium | add explicit `Do NOT use for` on both sides |
| `要件定義` | `interviewing-issues`, `spec` | Medium | clarify Trigger ("曖昧 Issue 明確化" vs "PRD として書き下ろし") |

### Notable partial-include pairs (boundary risk)

| ⊂ | ⊃ | Severity |
|---|---|----------|
| `'コンポーネント'` (frontend-design) | `'コンポーネント設計パターン'` (vercel-composition-patterns) | Low |
| `'gemini'` (gemini) | `'gemini api'` / `'gemini sdk'` (gemini-api) | Low (already disambiguated) |
| `'review'` (review) | `'PR review'` (github-pr), `'security-review'` (security-review), `'output-review'` (output-review), `'review my UI'` (web-design-guidelines), `'weekly review'` (weekly-review) | Medium — generic "review" risks misrouting |
| `'技術的負債'` (refactor-session) | `'技術的負債の洗い出し'` (audit) | Low |
| `'tech debt'` (refactor-session) | `'tech debt audit'` (audit) | Low |

### Mutual-exclusion gap — frontend-design ↔ ui-ux-pro-max

Both skills target UI work. Neither lists the other in `Do NOT use for`:

| Skill | Triggers | `Do NOT use for` |
|-------|----------|------------------|
| `frontend-design` | `'UI作って'`, `'ページ作成'`, `'コンポーネント'`, `'デザイン改善'`, `'UI磨いて'`, `'polish'`, `'distill'`, `'UIがダサい'`, `'AI臭い'` | `/web-design-guidelines`, `/senior-frontend`, `/react-best-practices` |
| `ui-ux-pro-max` | `'UI設計'`, `'デザイン'`, `'カラーパレット'`, `'フォント選定'`, `'アクセシビリティ'`, `'チャート'` | `senior-frontend`, `react-best-practices` |

**Lexical overlap is zero**, semantic overlap is **high** (both fire on "UI/デザイン" intents). Same gap flagged 2026-05-27 and unresolved. Recommend explicit territory split:

- `frontend-design` = **bespoke aesthetic direction + Refine ops** (polish, distill, critique, energize, calm)
- `ui-ux-pro-max` = **system / a11y / KB-lookup** (color palettes, font pairings, 99 UX rules, 25 chart types, accessibility audit)

Add to each `Do NOT use for`:
- frontend-design: `デザインシステム/a11y/カラーパレット選定 (use /ui-ux-pro-max)`
- ui-ux-pro-max: `Refine pass / polish / distill / AI臭い解毒 (use /frontend-design)`

---

## Gotchas Coverage Scan

| Metric | Value |
|--------|-------|
| Skills total | 122 |
| Skills with `## Gotchas` | 17 |
| Coverage | **13.9%** 🚨 |
| Coverage threshold (audit policy) | 40% |
| Gap | 105 skills missing `## Gotchas` |

### Coverage status of A/B targets

| Skill | Has `## Gotchas`? |
|-------|------------------|
| `react-best-practices` | ❌ |
| `frontend-design` | ❌ |
| `ui-ux-pro-max` | ❌ |
| `review` | ✅ |

### Recommended Gotchas to add (from `references/lessons-learned.md` and memory)

| Skill | Source line | Suggested Gotcha |
|-------|-------------|------------------|
| `frontend-design` | `lessons-learned`: "ui-ux-pro-max と境界が曖昧" | "ui-ux-pro-max と同時起動しない — Refine ops と KB-lookup は排他" |
| `ui-ux-pro-max` | `memory MEMORY.md` family教訓 | "10-stack KB は CLI 経由で domain 指定。一括引きは context 浪費" |
| `react-best-practices` | `feedback_codex_searching_phase` | "perf rule 提案前に Server Components / RSC モードを確認 — 同名 hook が App Router で挙動違う" |
| `review` (existing has 1 Gotcha) | additional | "worktree 内では symlink 越しに main repo を読み false positive 出す ([feedback_worktree_review_symlink_stale](../../.claude/projects/-Users-takeuchishougo-dotfiles/memory/feedback_worktree_review_symlink_stale.md))" |

---

## Stale `/senior-frontend` reference

Filesystem check: `~/.claude/skills/senior-frontend/` **does not exist**.

Skills still referencing it in `Do NOT use for`:

| Skill | Reference |
|-------|-----------|
| `react-best-practices` | "アーキ決定 (`/senior-frontend`)" |
| `frontend-design` | "フロントエンドアーキテクチャ（use `/senior-frontend`）" |
| `ui-ux-pro-max` | "frontend architecture decisions (use senior-frontend)" |

Same stale ref flagged in [2026-05-27 audit](2026-05-27-audit.md) — **unresolved for ~3 weeks**.

**Action**: either (a) restore the `senior-frontend` skill, (b) repoint to `react-best-practices` / `audit`, or (c) drop the exclusion clause.

---

## Agent Consolidation Hint

Agent inventory in `~/.claude/agents/`: **21 agents**.

Review-flavored agents (14/21 = **67%**):

```
code-reviewer, codex-plan-reviewer, codex-reviewer, comment-analyzer,
cross-file-reviewer, design-reviewer, edge-case-hunter, golang-reviewer,
product-reviewer, security-reviewer, silent-failure-hunter, test-analyzer,
type-design-analyzer, typescript-reviewer
```

This is a known dotfiles strength (per-perspective parallel review) but worth a periodic orthogonality check that each agent's recommendations are non-overlapping. Full consolidation scan deferred to next dedicated `/skill-audit consolidation` run.

---

## A/B Benchmark Results

**Status: FAILED — all 4 background subagents hit a session limit during the blind grading phase** (`subagent_tokens` 335–520 each, reset at 18:40 JST). `run_eval.sh` completed for all 4 (24 `response.md` files exist with real content), but `compare.sh` — which also spawns `claude -p` for blind grading — could only finish 3 of 12 grades before quota exhaustion. `aggregate.py` / `emit_benchmark.py` never ran. Iteration directories preserved: `react-best-practices/iteration-7`, `frontend-design/iteration-4`, `ui-ux-pro-max/iteration-3`, `review/iteration-6`.

### Partial grading available (3/12)

| Skill | Eval | Quality (with) | Quality (baseline) | Delta | Source |
|-------|------|---------------|-------------------|-------|--------|
| `react-best-practices` | clear-trigger | 7 | 9 | **−2** | iteration-7 |
| `react-best-practices` | borderline | 8 | 8 | 0 | iteration-7 |
| `react-best-practices` | domain-depth | — | — | — | response.md only |
| `ui-ux-pro-max` | clear-trigger | 9 | 8 | **+1** | iteration-3 |
| `ui-ux-pro-max` | borderline / domain-depth | — | — | — | response.md only |
| `frontend-design` | all 3 | — | — | — | response.md only |
| `review` | all 3 | — | — | — | response.md only |

**Statistical caveat**: per skill audit policy ("minimum 5 runs, ideally 10+"), these N=1–2 samples are not significant. Treat as anecdotal hint only.

### Anecdotal signals (use with caution)

- `react-best-practices` clear-trigger (waterfall-elimination prompt): the **baseline outperformed the with-skill** answer (9 vs 7). Grader rated the without-skill output more diverse (Promise.all / Suspense streaming / React.cache dedup) while the with-skill output had two overlapping techniques. Suggests the 40+ rule reference may push the model toward enumerating rules rather than picking distinct angles.
- `ui-ux-pro-max` clear-trigger (Tailwind+shadcn palette + WCAG AA): with-skill marginally helped (9 vs 8) — all 4 grader assertions passed in the with-skill output (CSS custom properties, light/dark, WCAG AA, semantic tokens). Confirms KB lookup adds value on system-level tasks.
- No data for `frontend-design` and `review` — too few signals to draw any conclusion.

---

## Recommendations

### Retire

(none today — A/B may add candidates; static scan alone does not justify retirement)

### Improve

- **`react-best-practices`** — (a) rewrite description to outcome-first verb (e.g. "Cut React/Next.js perf bottlenecks: waterfalls, bundle bloat, render churn"), (b) add `## Gotchas`, (c) drop or repoint `/senior-frontend` reference.
- **`frontend-design`** — (a) add explicit `Do NOT use for: …デザインシステム/a11y (use /ui-ux-pro-max)` to declare boundary with `ui-ux-pro-max`, (b) add `## Gotchas` (esp. "Refine ops collide with system-level decisions"), (c) drop `/senior-frontend`.
- **`ui-ux-pro-max`** — (a) add explicit `Do NOT use for: Refine pass / polish (use /frontend-design)`, (b) trim description from 471 → ≤200 chars (drop the 10-stack enumeration; keep in body), (c) add `## Gotchas`, (d) drop `/senior-frontend`.
- **`review`** — keep current scope. Optional: extend `## Gotchas` with worktree-symlink false-positive warning.

### Cross-cutting (priority over individual improvements)

1. **Description token tax** — open follow-up to trim the Top 10 (≥460 chars each). Even bringing each to 250 chars saves ~3,500 chars from system prompt.
2. **Gotchas coverage 14% → 40%** — make `## Gotchas` mandatory in `/skill-creator` template; backfill the Top 20 most-used skills first (requires `skill-executions.jsonl`).
3. **Restore `skill-executions.jsonl` capture** — without it, Usage Tier and Dominant/Unused gating is blind.
4. **Frontend boundary** — same gap as 2026-05-27. Either declare boundary or merge skills.

---

## Method notes

- Default Batch 1+2 referenced 10 skills; **6 not found** (`react-expert`, `senior-architect`, `senior-backend`, `senior-frontend`, `search-first`, `codex-review`). Scope auto-adjusted to 4 existing skills + full static scan. Update the `Default Target Batches` section of `~/.claude/skills/skill-audit/SKILL.md` to match reality, or skip outright when items are absent.
- Trigger phrase extraction used quoted strings inside `Triggers:` blocks; YAML block scalars are normalised by collapsing folded lines.
- Composition depth counted explicit `Skill(...)` / `Agent(...)` / `subagent_type` references in SKILL.md text only. Runtime chains (e.g. `review` orchestrating code-reviewer through hook policy) are noted in the table but not counted toward the depth-9 ceiling.

## Open questions / follow-ups

1. Stale `/senior-frontend` is the same finding as 2026-05-27 — what's the resolution policy on stale skill references?
2. `skill-executions.jsonl` capture path moved or stopped — needs to be wired back for any usage-aware auditing.
3. Worth changing `skill-creator` template to enforce `## Gotchas` section? Coverage is at 14% which suggests current template doesn't ask for it.
4. **Subagent + `claude -p` session sharing**: 4 parallel `general-purpose` subagents each spawning `claude -p` for run_eval + compare exhausted the per-session quota. Need either (a) serialize A/B runs, (b) move A/B to a separate non-Anthropic-quota grader (Codex `gpt-5.5` as grader), or (c) run A/B from a non-Claude-Code shell. The bug from the 2026-05-27 audit ("subagent-driven A/B runs stalled at watchdog due to nested `claude -p`") is structurally still present.
5. **Resume A/B after quota reset (18:40 JST)**: 24 `response.md` outputs are preserved in today's iteration dirs. Re-running just `compare.sh` + `aggregate.py` after reset would complete the dataset without burning another generation pass.
