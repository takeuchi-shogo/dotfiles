# Phase B1.5 — Bootstrap Declarative Retention (homebrew-in-nix)

Status: completed (2026-04-25)
Created: 2026-04-25
Supersedes-attempt: Phase B1 Step 7 (commit e6753d9) / rolled back by 9887dbe
Ref: `docs/plans/active/2026-04-24-nix-migration-phase-b1-plan.md`

## Background

Phase B1 Step 7 で bootstrap 5 ツール (git, sheldon, starship, mise, direnv) を nix 管理に移したが、以下で失敗し 9887dbe で rollback:

- direnv `checkPhase` が Determinate Nix + sudo + Apple Silicon 環境でハング
- `overrideAttrs (doCheck = false)` は Hydra cache 無効化 → 毎 switch で go-modules 再ビルド (5+ 分)

## Strategy Decision

Codex deep reasoning (gpt-5.4 xhigh) で 4 案を評価した結果:

| Strategy | Verdict |
|---|---|
| 1. Upstream patch (nixpkgs に `doCheck = lib.mkDefault false` PR) | 長期解だが blocker 解消には遅い |
| 2. Per-package overlay (`overlays/direnv.nix` で patches 側 test skip) | overrideAttrs よりマシだが patch 追従が SLO 違反 |
| 3. `nix profile` only (home-manager から切り出し) | `darwin-rebuild` rollback と canonical source を破壊 |
| **4. homebrew-in-nix (`nix-darwin.homebrew.brews`)** | **推奨**: 宣言性を保ったまま brew に委譲 |

**Selected**: Hybrid **4 + 1**
- 主戦略: Strategy 4 で 5 ツールを `nix-darwin.homebrew.brews` に宣言
- 補助戦略: Strategy 1 で direnv hang を upstream issue 化（merge 待ちはしない）

## Rationale

- 既存 master plan D9 で「Homebrew を nix-darwin 経由で宣言的に残す」方針が明記済み
- Strategy 4 は「妥協」ではなく「計画内の例外処理」
- canonical source が flake 側に一元化され、現状の「Brewfile + nix の二重管理」を解消

## Execution Plan

### Subphase B1.5.1 — `bootstrap-retain` 公式化

- `nix/darwin/default.nix` の `homebrew.brews` に `git`, `sheldon`, `starship`, `mise`, `direnv` を追加
- Phase B1 inventory で 5 ツールを `brew-retain` 再分類
- **DoD**: `task nix:switch PROFILE=private` warm run ≤ 5 分、hidden Go rebuild なし

### Subphase B1.5.2 — Canonical source 寄せ替え

- `Brewfile` から 5 ツール行を削除（flake が唯一の権威）
- `nix/darwin/default.nix` に「5 ツールは意図的に brew-retain」と comment
- **DoD**: 新 shell で 5 ツール安定起動、PATH 回帰なし、`brew bundle check` satisfied

### Subphase B1.5.3 — Upstream follow-up（並走、非 blocking）

- direnv hang を nixpkgs issue/PR 化
- merge / binary cache 改善を期待するが待たない
- **DoD**: repo 内に custom patch が存在しない

## Rollback Criteria

以下のいずれかで即 abort:

1. 1 ツールあたり調査/修正が **2 時間超**
2. warm `nix:switch` が **5 分超を 2 回連続**
3. hidden source rebuild、PATH 回帰、shell 起動劣化の再発

いずれかを踏んだら、そのツールは即 `brew-retain` に戻し、bootstrap set 全体の再 nix 化は次の upstream 改善まで凍結。

## Success Criteria

- [x] `nix/darwin/default.nix` の `homebrew.brews` に 5 ツール宣言済み (commit 7504f29)
- [x] `Brewfile` の bootstrap 5 行削除済み (commit 7504f29)
- [x] `task nix:switch PROFILE=private` 完走 (warm run ~1 min、direnv checkPhase hang 再発なし)
- [x] `command -v git sheldon starship mise direnv` 全て解決 (git/sheldon/starship/direnv は brew Cellar、mise は shell function)
- [x] 新 shell で `starship` prompt 表示、`direnv allow` 動作、`mise activate` 動作 (現セッションで既に稼働確認、switch で regression なし)
- [x] `brew bundle check --file=~/dotfiles/Brewfile` satisfied (font 2 個のみ)

## Outcome

- `nix-darwin.homebrew.brews` に 5 ツール追加 → nix が生成する Brewfile に反映
- `~/dotfiles/Brewfile` は font 2 個のみに縮退（Phase C まで持ち越し）
- `brew bundle` 出力で 19 dependencies (cask 9 + tap 3 + brews 7 = borders/mo/git/sheldon/starship/mise/direnv)
- canonical source が flake 一元化、二重管理解消
- Strategy 1 (upstream direnv patch) は追加のタスクとして将来検討（現時点では non-blocking）

## References

- Codex strategy selection: gpt-5.4 xhigh (agentId: a5072844dfb782b54)
- Phase B1 master: `docs/plans/active/2026-04-24-nix-migration-phase-b1-plan.md`
- rollback commit: `9887dbe`
