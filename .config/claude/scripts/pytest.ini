[pytest]
# scripts/ ルートで bare `pytest` を実行したときに全テストを確実に discover させる。
# これが無いと "No tests collected" となり、壊れたテストが静かに見過ごされる
# (回帰検知の盲点)。testpaths に tests/ と root の test_skills.py を明示する。
testpaths = tests test_skills.py
python_files = test_*.py
