---
description: "Validate implementation against spec acceptance criteria"
allowed-tools: Read, Bash, Glob, Grep, Agent
---

# /validate

実装を spec の acceptance criteria で検証する: $ARGUMENTS

validate スキルを使用して Product Validation を実行する。

1. 引数あり: 指定された spec ファイルで検証
2. 引数なし: docs/specs/ から自動検出
