/ralph-loop:ralph-loop "
既にある変更をレビューし、品質基準を満たすまで修正を繰り返す。

対象: $ARGUMENTS

## 手順

1. `git diff --stat HEAD` で現在の変更を確認する
2. `.tmp/review-feedback.md` が存在すれば読み込み、前回の指摘に対応した修正を行う
3. 変更内容をレビューする:
   - 正確性: ロジックの正しさ、エッジケース
   - スタイル: 命名、フォーマット、言語の慣例
   - セキュリティ: インジェクション、認証漏れ
   - 影響範囲: 変更が他ファイルに与える影響
   - テスト: 変更に対するテストの網羅性
4. テストを実行し、全テストが pass することを確認する
5. レビュー結果を `.tmp/review-feedback.md` に書き出す:
   - PASS: 全項目問題なし → `<promise>APPROVED</promise>` を出力
   - NEEDS_FIX: 指摘あり → 修正を実施し、ステップ 3 に戻る

## レビュー基準

- テストが全て pass すること
- 新しい lint 警告がないこと
- セキュリティ上の問題がないこと
- コードの意図が明確であること
- 不要な変更が含まれていないこと

## 重要

- 実装は行わない。既存の変更に対するレビューと修正のみ
- 各イテレーションで修正した内容を簡潔にログに残す
- 同じ指摘を2回以上繰り返す場合はアプローチを変える
- 修正の範囲はレビュー指摘に直接関係する箇所のみ。スコープを広げない
" --completion-promise "APPROVED" --max-iterations 10
